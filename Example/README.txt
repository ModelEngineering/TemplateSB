Examples of running the template pre-processor.
To run the demo, run the bash script demo.sh.
This script reads the template in sample.tmpl, creates the
antimony file sample.mdl, and then runs the model.
